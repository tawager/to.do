/**
 * Created by a_wags25 on 10/20/14.
 */

/*function maxVals(array, num) {
    var mins = [];
    for(i= 0; i< array.length; i++){
        if (num>=array[i]) {
            mins.push (array[i]);
        }
    }
  return mins;
}

 console.log (maxVals([1,3,8,9,4,2,10,7],5));*/

    array.filter (function(value, index) {
        return  (value <= max);
    })

//make a new array out of numbers less than the max
